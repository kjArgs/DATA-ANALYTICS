## EXERCISE NO 1

    1. CREATE A DATABASE NAMED Sql_Sample.

    2. CREATE A TABLE NAMED celebs with this values (id, INTEGER), (name, STRING), and (age, INTEGER).

    3. INSERT INTO TABLE celebs with this value (1, 'Justin Bieber', 29).

    4. ADD 3 MORE WITH THESE VALUES (2, 'Beyonce Knowles', 42), (3, 'Jeremy Lin', 35), (4, 'Taylor Swift', 33).

    5. DISPLAY THE NAMES FROM TABLE celebs.

    6. DISPLAY ALL THE DATA FROM TABLE celebs.

    7. MODIFY TABLE celebs, add a new column named twitter_handle with a datatype of a string.

    8. Update the record with an id value of 4 to have the twitter_handle @taylorswift13.

    9. Delete all of the rows that have a NULL value in the twitter handle column.

    10. Create a new table named awards with the values such as id which is an integer and a primary key, recipient which is a text and must be not null, award name which is text with a default value of Grammy.

## EXERCISE NO 2
    1.Create a table named friends with three columns:
        id that stores INTEGER
        name that stores TEXT
        birthday that stores DATE

    2. add Ororo Munroe to friends her birthday is May 30th, 1940.

    3. Check if the table is created and if Ororo Munroe is added to the table.

    4. Add two more of your friends on the table.

    5. Change Ororo Munroe name to Storm.

    6. Add new column email

    7. Update Storm email address to storm@codecademy.com

    8. Remove storm to the friends table

    9. Check if storm is removed
